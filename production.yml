version: '3.2'
services:
    web:
        image: winzerhofwurst/web:latest
        ports:
            - "8080:80"
    app:
        image: winzerhofwurst/app:latest
        env_file: .env.prod
    database:
        volumes:
            - ./maria-prod:/var/lib/mysql

